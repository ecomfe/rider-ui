// Rider UI Core
//
// @desc    内容块
// @author  firede(firede@firede.us)


@require 'setting'


// mixins
// ================

ui-content-style()
    h1, h2, h3, h4, h5, h6,
    strong
        font-weight: 700
    em
        font-style: italic
    code
        font-family: monospace
    pre
        overflow-x: auto


// vertical rhythm
ui-content-rhythm($font-size = 16px, $line-height = 26px, $ui-heading-ratio = (2.75 2.25 1.75 1.25 1 0.85))
    font-size: rem($font-size)
    line-height: rem($line-height)

    // set heading font-size
    $ui-heading-length = length($ui-heading-ratio)
    for $ui-cur in (1..$ui-heading-length)
        $ui-cur-ratio = $ui-heading-ratio[$ui-cur - 1]
        $ui-cur-size = $ui-cur-ratio * $font-size
        h{$ui-cur}
            font-size: rem($ui-cur-size)
            line-height: rem(ceil($font-size * $ui-cur-ratio / $line-height) * $line-height)

    h1, h2, h3, h4, h5, h6
        margin: rem($line-height / 2) _

    // list
    ul, ol
        margin: rem($line-height / 2) 0 rem($line-height / 2) rem(25)
    ul
        list-style-type: disc
    ol
        list-style-type: decimal
    ul ul,
    ul ol,
    ol ol,
    ol ul
        margin: 0 _
